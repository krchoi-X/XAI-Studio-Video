# Shared new-session creation records

Active editor: Codex / Astra. Studio implementation assigned to Terra.
Goal: new character session records, inputs and runs share the existing Library session folder with outputs, independently of application checkouts.
Contract: Private CREATION_RECORD_CONTRACT.md at 6ea0efd. Preserve the previous staged shared-authority patch and unrelated untracked production work.
Scope: Character Manager path helpers, scene/face/reference-variation producers, input snapshots, session resume validation, artifact contract and focused fixtures. Reuse recorder runs_root behavior.
Constraints: all old sessions stay in place; IDs/asset roots/reviews/DNA unchanged. No generation, live Sync, deletion or public push. Fail closed on conflicts. The contract amendment permits bounded Studio adoption after idle-job and backup gates; no GPU restart.
Plan: compatible helpers; new writer and snapshots; synthetic old/new/resume/recorder tests; Studio integration; read-only actual path proof; activate catalog after gates; preserve exact cumulative private patch if public checker fails.
Contract impact: producers choose one shared session folder, recorder writes runs underneath, workers pass explicit returned paths, Studio reads legacy and shared manifests. Old records are never silently moved. Input snapshots are history, not editable DNA/skills. Before new production rollback restores catalog; afterward keep shared readers and reconcile writers.
Progress: runtime focused tests 74 passed; Studio focused tests 23 passed (Studio 7fe756b). Studio adopted the reader through its existing supervisor after idle-job checks and a consistent DB backup; new PID 49256 is healthy. Shared-new-sessions is active. Both project resolvers select the same Library; all DB row fingerprints, 12 DNA hashes and 116 legacy session/433 run records remain unchanged. No live production test or Sync was run. Private evidence: creation-activation-before/adopted/active.json. Another agent committed earlier staged shared-authority changes in 06c6041; preserve that history and checkpoint the current diff against fresh HEAD.
Next: historical session/standalone-run, curated-set and training-set writer transitions require separate contracts. The active new writer does not migrate those records. Keep shared readers after the first shared session; never roll back to readers that hide it. Final publication evidence and exact patch live in Private creation-records-public-check.json and creation-records-xai.patch.
Blockers: none for implementation. Publication gate still prevents public commits; use exact private patch checkpoint.
