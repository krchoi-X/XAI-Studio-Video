# Shared immutable character sets

Active editor: Codex / Terra (bounded runtime implementation); integration owner Astra.
Goal: preserve existing sets, create fresh shared identity/LoRA versions, and expose a common lightweight listing.
Contract: Private docs/architecture/stage-3/CHARACTER_SET_CONTRACT.md (read committed file before implementation).
Scope: tools/character_sets.py, identity_set_builder.py, lora_dataset.py, shared_resources.py, tests/test_character_sets.py, this task only. Preserve staged creation-record work.
Constraints: no existing set deletion/overwrite, no live build/analysis/GPU/Sync, no inferred approval/canonical latest set, no service change or public commit.
Contract impact: old schema-1 manifests remain readable; new optional context sidecar; CLI output options gain shared defaults but reject existing paths; runtime listing is shared with Studio. Failed builds retain incomplete directories without final manifests. Rollback never restores destructive rerun behavior.
Plan: inspect exact producer/consumer semantics; immutable shared destination and context helper; adapt builders/CLI; synthetic preservation and compatibility tests; compact handoff.
Progress: implementation complete pending integration review. `character_sets.describe_sets(character_id)` uses the sibling Character Manager by file path, returns no current-set election, validates complete schema-1 identity/LoRA sets and rejects unsafe members or declared caption-sidecar mismatches. Builders reserve immutable destinations before work, never unlink prior members, leave failed partial folders without final manifests, and publish context/input snapshots before their final manifest. Context captures current DNA as `context_only`, preserving argument value types and exact source-manifest bytes/hashes.

Verification: from `D:/codex/XAI-studio`, with `PYTHONPATH` set to `tools` plus the existing Hermes site-packages only to supply NumPy, and interpreter `D:/codex/personal-prompt-studio/personal-prompt-studio/backend/.venv/Scripts/python.exe`, `-m pytest tests/test_character_sets.py -q --basetemp .pytest_tmp/character-sets-builders-0915b` passed 9 tests. `py_compile` passed all four changed tools. Synthetic coverage includes schema-1 discovery, incomplete folders, unsafe members, caption mismatches, immutable reserve, exact source/DNA snapshot hashes and mutation rejection, plus successful identity→LoRA writer publication with real final manifest writes, context/snapshot/output hashes/caption sidecars, immutable rerun preservation, and partial failure cases for duplicate names and existing sheets. Recognizer/model work remained stubbed.

Integration accepted 2026-09-15: runtime selection (sets, shared authority, creation records) 30 passed; Studio authority/sets/video selection 29 passed. Studio d3a06ae adopted through existing supervisor after idle-job and exact DB-backup checks. Runtime, Studio code and live API agree for all 12 characters and five existing sets; all 1,016 existing set files match retained stat/text-hash baseline and all 16 DB tables are unchanged. Atomic final manifest publication requires same-filesystem hard links (verified on this Windows volume); failure retains an unfinished folder. No live dataset build, training, Sync, source deletion or public commit. Exact runtime patch and publication receipt are preserved in Private character-sets-xai.patch / character-sets-public-check.json.
Next: historical session/report and standalone-run bundle migration, unresolved old references, and full Library media backup verification. Never restore destructive rerun behavior on rollback; old sets remain valid at their original shared paths.
Blockers: none; existing public publication gate uses Private exact patch checkpoint.
