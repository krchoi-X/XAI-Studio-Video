# Grok Image Runtime Prompt

- variant: common-prompt, user-reported
- provider: xAI Grok
- service/model: Grok Image; internal version not exposed
- source canonical revision: v1
- generated images: 4
- resolution: 1152x1728
- seed: not exposed
- generation settings: not exposed

## Submitted prompt provenance

The user reports using canonical prompt v1 for this four-image generation. Unlike the WanGP and Venice outputs, these JPEGs do not embed the prompt or generation settings. They contain only an image UUID in EXIF `Artist` plus a signed provenance payload in `ImageDescription` and `UserComment`.

- canonical source used by user: `../canonical-prompt.md`
- prompt equality status: user-reported, not independently verifiable from the files
- image UUIDs: recorded in `../batch.yaml`
